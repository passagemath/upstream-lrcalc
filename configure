#!/bin/sh

if [ ! -d bin ]; then
  mkdir bin
fi

echo 'LRCALC_BIN_PATH := `'"`pwd`/bin"'`:' >> lrcalc

cd mathlib
make

cd ../lrcoef
make
mv lrcalc sat lrcoef skew mult coprod lrskew ../bin

cd ../schubert
make
mv schubmult ../bin

